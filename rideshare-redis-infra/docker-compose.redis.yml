
version: "3.9"

services:
  swiftride-redis:
    image: redis:7-alpine
    container_name: swiftride-redis
    command: redis-server --requirepass ${REDIS_PASSWORD}
    ports:
      - "6379:6379"    
    networks:
      - swiftride-net

networks:
  swiftride-net:
    external: true   

